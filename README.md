# text-inflator
A simple that starts with a .txt with letters that get converted into bigger strings of other characters, recursively expanding them until the output file is massive. There is one version that uses memoization, and the other just simple string manipulation.


